#define for if(0); else for 
#include<stdio.h>
#include<stdlib.h>

#include<string.h>
#include<string>
#include<ctype.h>

#include<algorithm>
#include<math.h>>//有cmath 但是没有math

#include<stack>
#include<queue>
#include<vector>// add graghs  
#include<map>

#include<time.h>
using namespace std;

vector< pair<int,int> > v;
int main(){
	//freopen("in.txt","r",stdin);   
	pair<int,int> p1(23,24),p2(66,77);
	p2.swap(p1);
	v.push_back(make_pair(1,2));
	v.push_back(p1);
	return 0;
}

