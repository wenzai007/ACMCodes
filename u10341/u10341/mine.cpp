#define for if(0); else for 
#include<stdio.h>
#include<stdlib.h>

#include<string.h>
#include<string>
#include<ctype.h>

#include<algorithm>
#include<math.h>//有cmath 但是没有math

#include<stack>
#include<queue>
#include<vector>// add graghs  
#include<map>

#include<time.h>
using namespace std;

int p,q,r,s,t,u;
int main(){
	freopen("in.txt","r",stdin);   
	int cnum;
	char ins[20];
	while (gets(ins)){
		sscanf(ins,"%d%d%d%d%d%d",&p,&q,&r,&s,&t,&u);
	}
	return 0;
}

